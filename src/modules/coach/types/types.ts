export interface Athlete {
    id: number;
    complete_name: string;
    email: string;
    birthdate: string;
    // created_at: string;
    // active: boolean;
    // height: number;
    // weight: number;
}